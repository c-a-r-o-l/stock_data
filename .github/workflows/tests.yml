name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  leakage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pytest pyyaml pandas pyarrow

      - name: Run leakage tests
        run: |
          PYTHONPATH=src python -m pytest tests/ -v -m leakage --strict-markers --tb=long
